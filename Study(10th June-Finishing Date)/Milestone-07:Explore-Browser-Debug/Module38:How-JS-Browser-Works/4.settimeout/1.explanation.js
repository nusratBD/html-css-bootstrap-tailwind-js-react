/* 
setTimeout: settimeout is a function which places the attributed code(inside of it) at the last for execution. setTimeout() does the compilation asynchronous.

As a parameter, it takes function and miliseconds.
*/