/* 
local storage: After refreshing the browser, data will not destroy from here.

session storage: After every refreshing, data lose from here.

indexdDB, Web SQL: Database

Cookies: small data storage, a little bit different from local and session storage.

Cache Storage: When we visit any website for the first time, it saves all the raw code in it. As a result, at next visit it doesn't load all the raw code rather it shows from cache. In this way, it increases the performance(reducing load time and show data quickly) of the website.
*/